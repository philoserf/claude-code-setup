# Valid input test cases for parameter-validation script

# Basic required parameters
--input test.txt --output result.txt

# With optional count
--input test.txt --output result.txt --count 10

# With format
--input test.txt --output result.txt --format csv

# With verbose
--input test.txt --output result.txt --verbose

# All options
--input test.txt --output result.txt --count 50 --format xml --verbose

# Edge cases - minimum count
--input test.txt --output result.txt --count 1

# Edge cases - maximum count
--input test.txt --output result.txt --count 100
